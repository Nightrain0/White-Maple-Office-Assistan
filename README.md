# White Maple Office Assistant (白枫办公助手) 🍁

[](https://www.google.com/search?q=LICENSE)
[](https://mp.weixin.qq.com/)

**白枫办公助手** 是一款基于微信原生开发 + 微信云开发 (CloudBase) 的全能型办公效率工具小程序。旨在为用户提供便捷的图像处理和文字识别服务，无需下载繁琐的 App，即用即走。

## ✨ 核心功能 (Features)

项目包含六大核心模块，覆盖日常办公高频场景：

1.  **📷 智能证件照 (Smart ID Photo)**

      * 支持拍摄或上传照片，自动进行人像抠图。
      * 提供多种标准尺寸（1寸、2寸、签证、教师资格证等）裁剪。
      * 一键更换底色（红、蓝、白、灰）。
      * 基于百度 AI 人像分割与图像融合技术。

2.  **📉 图片压缩 (Image Compress)**

      * 支持指定目标文件大小（KB）进行压缩。
      * 支持修改图片分辨率（宽高缩放）。
      * 直观对比压缩前后的体积与尺寸变化。

3.  **📚 批量处理 (Batch Processing)**

      * 支持一次性导入多张图片（最多9张）。
      * 统一设置压缩参数或尺寸，队列式自动处理。
      * 处理完成后一键保存所有图片到相册。

4.  **🛡️ 安全水印 (Watermark)**

      * 为证件或敏感图片添加全屏水印。
      * 自定义水印文字、字号、颜色、透明度。
      * 支持调节水印密度与旋转角度。
      * 纯前端 Canvas 绘制，保护隐私不上传。

5.  **🧩 长图拼接 (Stitch)**

      * 支持三种拼接模式：纵向拼接、横向拼接、宫格拼接。
      * 自定义背景颜色、边距 (Padding)、间距 (Gap) 和圆角。
      * 支持生成高清长图。

6.  **📝 文字识别 (OCR)**

      * 支持通用印刷体、身份证、银行卡、驾驶证、营业执照等多种识别模式。
      * **双引擎支持**：
          * 基础模式：使用微信服务市场 OCR 插件。
          * AI 模式：通过云函数调用大模型接口，支持手写体智能纠错。

## 🛠️ 技术栈 (Tech Stack)

  * **前端**：WXML, WXSS, JavaScript (微信小程序原生)
  * **后端**：微信云开发 (Cloud Functions)
  * **图像处理**：Canvas 2D API, `Jimp` (Node.js 图像处理库)
  * **AI 能力**：
      * 百度 AI 开放平台 (人像分割)
      * Remove.bg API (备用抠图方案)
      * 大语言模型 API (用于复杂手写 OCR 纠错)

## 🚀 快速开始 (Getting Started)

### 1\. 环境准备

  * 下载并安装 [微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)。
  * 注册微信小程序账号，并开通**云开发**环境。

### 2\. 本地配置

1.  克隆本项目到本地：
    ```bash
    git clone https://github.com/your-username/white-maple-office.git
    ```
2.  在微信开发者工具中导入项目根目录。
3.  修改 `project.config.json` 中的 `appid` 为你自己的小程序 AppID。
4.  修改 `miniprogram/app.js` 中的 `env` 为你的云开发环境 ID。

### 3\. 云函数部署

本项目依赖三个核心云函数，请在开发者工具的“云开发”面板中上传并部署：

  * **aiOcr**: 处理 AI 文字识别。
      * 进入目录 `cloudfunctions/aiOcr`，运行 `npm install`。
      * 右键上传并部署。
  * **baiduSeg**: 处理人像分割。
      * 进入目录 `cloudfunctions/baiduSeg`，运行 `npm install`。
      * 右键上传并部署。
  * **changeBg**: 处理证件照换底色。
      * 进入目录 `cloudfunctions/changeBg`，运行 `npm install`。
      * 右键上传并部署。

### 4\. 环境变量配置 (关键) 🔑

为了保护密钥安全，请不要将密钥硬编码在代码中。请在微信开发者工具 -\> 云开发 -\> 云函数 -\> **版本与配置** 中，为对应的云函数添加以下环境变量：

| 云函数 | 变量名 | 说明 | 获取方式 |
| :--- | :--- | :--- | :--- |
| **aiOcr** | `API_URL` | 大模型 API 地址 | OpenAI / 国内大模型厂商 |
| **aiOcr** | `API_KEY` | 大模型 API Key | 同上 |
| **aiOcr** | `MODEL` | 模型名称 (如 gpt-4o) | 同上 |
| **baiduSeg** | `BAIDU_AK` | 百度 AI Access Key | [百度智能云控制台](https://console.bce.baidu.com/) |
| **baiduSeg** | `BAIDU_SK` | 百度 AI Secret Key | 同上 |
| **changeBg** | `REMOVE_BG_KEY`| Remove.bg API Key | [Remove.bg 官网](https://www.remove.bg/) |

### 5\. 第三方服务配置

  * **微信 OCR 插件**：在微信小程序后台 -\> 设置 -\> 第三方设置 -\> 插件管理中，添加“OCR支持”插件 (AppID: `wx79ac3de8be320b71`)。

## 📂 目录结构

```
├── cloudfunctions/        # 云函数目录
│   ├── aiOcr/             # AI 文字识别与纠错
│   ├── baiduSeg/          # 百度人像分割
│   └── changeBg/          # 证件照换底色
├── miniprogram/           # 小程序前端代码
│   ├── components/        # 公共组件
│   ├── images/            # 静态资源
│   ├── pages/             # 页面文件
│   │   ├── index/         # 首页
│   │   ├── idphoto/       # 智能证件照
│   │   ├── compress/      # 图片压缩
│   │   ├── batch/         # 批量处理
│   │   ├── watermark/     # 安全水印
│   │   ├── stitch/        # 长图拼接
│   │   └── ocr/           # 文字识别
│   ├── app.js             # 全局逻辑
│   └── app.json           # 全局配置
└── project.config.json    # 项目配置文件
```

## 🤝 贡献 (Contributing)

欢迎提交 Issues 和 Pull Requests 来改进这个项目！

## 📄 许可证 (License)

MIT License
